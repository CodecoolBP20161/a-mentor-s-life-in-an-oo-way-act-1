class Feedback():
    def __init__(self, name):
        self.name = name

    def give_feedback(self, target):
        # Change two object values (receiver and sender).
        return

    def get_feedback(self, target):
        # Change two object values (receiver and sender).
        return
